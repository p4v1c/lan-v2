id: nxc_ldap_anon
name: LDAP Anonymous Bind
category: "Enumération"
description: "Test de connexion LDAP anonyme (Null Bind)."
checklist_keys: ["anon_ldap"]
inputs:
  - name: target
    label: Cible (IP du DC)
    placeholder: 10.3.10.11
command: "nxc ldap {{target}} -u '' -p ''"

parsing:
  mode: "line"  
  rules:
    - name: "LDAP Null Bind (Anonyme)"
      regex: "\\[\\+\\]"
      badge: "🟠 [MOYEN]"