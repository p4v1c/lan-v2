id: nxc_ldap_checker
name: NetExec LDAP Security
category: nxc
description: "Vérifie la sécurité LDAP (Signing & Channel Binding) via le banner standard."
checklist_keys: ["sec_ldap"]
inputs:
  - name: target
    label: IP du DC
    placeholder: 10.3.10.11
  - name: username
    label: Utilisateur
    placeholder: guest
  - name: password
    label: Mot de passe
    placeholder: password
command: "nxc ldap {{target}} -u '{{username}}' -p '{{password}}'"

parsing:
  mode: "text"  
  rules:
    - name: "LDAP Signing NON Forcé (Relay Possible)"
      regex: "signing:(False|None)"
      badge: "🔴 [CRITIQUE]"
      
    - name: "Channel Binding Désactivé"
      regex: "channel binding:Never"
      badge: "🟠 [RISQUE]"
      