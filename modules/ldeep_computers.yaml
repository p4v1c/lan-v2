id: Enum Computers
name: Enum Computers
category: Enumération
checklist_keys: ["ad_computers"] # <--- AJOUTER ICI
description: ""
inputs:
  - name: domain
    label: "Domaine"
    placeholder: north.sevenkingdoms.local
  - name: dc_ip
    label: "IP du DC"
    placeholder: 10.3.10.11
  - name: username
    label: Utilisateur
    placeholder: user
  - name: password
    label: Mot de passe
    placeholder: password
command: "ldeep ldap -s ldap://{{dc_ip}} -d {{domain}} -u '{{username}}' -p '{{password}}' computers --resolve"

parsing:
  mode: "text"
  
  rules:
    - name: "Ordinateur Détecté"
      regex: "^\\d{1,3}(?:\\.\\d{1,3}){3}\\s+.*"
      badge: "💻 [HOST]"